using System.Collections.Generic;
using System.Linq;
using Machine.Specifications;

namespace CouchRS.Test
{
    public class when_flattening_object_with_one_complex_property
    {
        //static IEnumerable<IDictionary<string, string>> result;

        //Because of = () =>
        //    result = new JsonFlattener("{foo:{bacon:'sizzle'}").Flatten();

        //It should_have_one_combination = () => 
        //    result.Count().ShouldEqual(1);

        //It should_have_one_key_value_pair = () => 
        //    result.First().Count.ShouldEqual(1);

        //It should_have_correct_key_value_pair = () =>
        //    result
        //    .First() //First and only combination
        //    .First()
        //    .ShouldEqual(new KeyValuePair<string, string>("foo.bacon", "sizzle"));
    }
}